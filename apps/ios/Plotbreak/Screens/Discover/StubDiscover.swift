import SwiftUI

// Placeholder until the real screen is ported. Delete this file when it is.

struct DiscoverScreen: View { var body: some View { StubScreen(name: "Discover") } }
struct SearchScreen: View { var body: some View { StubScreen(name: "Search") } }

import SwiftUI

// Placeholder until the real screen is ported. Delete this file when it is.

struct StoryDetailScreen: View { let storyId: String; var body: some View { StubScreen(name: "StoryDetail") } }

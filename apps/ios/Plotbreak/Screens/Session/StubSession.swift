import SwiftUI

// Placeholder until the real screen is ported. Delete this file when it is.

struct SessionScreen: View { let sessionId: String; var body: some View { StubScreen(name: "Session") } }

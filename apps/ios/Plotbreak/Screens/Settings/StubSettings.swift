import SwiftUI

// Placeholder until the real screen is ported. Delete this file when it is.

struct SettingsScreen: View { var body: some View { StubScreen(name: "Settings") } }
struct PersonalizationScreen: View { var body: some View { StubScreen(name: "Personalization") } }
struct MyInformationScreen: View { var body: some View { StubScreen(name: "MyInformation") } }

import SwiftUI

// Placeholder until the real screen is ported. Delete this file when it is.

struct SignInScreen: View { var body: some View { StubScreen(name: "SignIn") } }
struct CreateScreen: View { var body: some View { StubScreen(name: "Create") } }
struct ReportScreen: View { let targetType: String; let targetId: String; var body: some View { StubScreen(name: "Report") } }
struct ReportHistoryScreen: View { var body: some View { StubScreen(name: "ReportHistory") } }
